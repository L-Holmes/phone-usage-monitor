# strong_sexual.txt   (weight: STRONG_WEIGHT, default 4)
# ---------------------------------------------------------------------------
# ALWAYS-ADULT words — they mean the adult thing ~99% of the time ("sex", "slut",
# "onlyfans", "naked", "smut"). Unlike dual-meaning words they do NOT need a
# sexual neighbour to count — they count on their own — but they carry a slightly
# lower weight than the hardcore explicit list, so a single stray use is still
# tolerated (with STRONG_WEIGHT 4 and THRESHOLD 10 you need ~3 in the body, or
# one in the title/URL plus one more signal, to block). They also act as
# "indicators", so e.g. "sex" next to a dual word like "teen" sexualises it.
#
# This is the "separate the words that are basically always adult" tier.
#
# CAVEAT: "sex" is the one word here with real innocent uses (sex education,
# "sex: male" on a form, "same-sex"). It needs to repeat to block, so a single
# mention is safe — but a sex-ed page that uses it a lot may trip. If that
# bothers you, move "sex" down to dual_meaning.txt (then it needs a sexual
# neighbour to count at all).
#
# FORMAT: one lowercase whole word per line; '#' and blank lines ignored.
# TUNING: behave like hardcore -> move up to explicit_sexual.txt; too eager ->
# move down to subtle.txt (weaker) or dual_meaning.txt (context-gated).
# ---------------------------------------------------------------------------
boob
boobies
boobs
hardon
naked
onlyfans
sex
slut
sluts
slutty
smut
topless
whore
whores
xrated
