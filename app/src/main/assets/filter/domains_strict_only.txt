# Hand-maintained hosts blocked ONLY in Strict / Super hardcore (allowed in Relaxed).
# One host per line. "#" starts a comment. A bare domain also covers its subdomains.
# LANGUAGE-NEUTRAL. Loaded by FilterData → StrictOnlyBlocklist.
#
# EMPTY on purpose (2026-08-01): the whole list - reddit frontends/mirrors, reddit itself,
# borderline shopping, imageboards - moved to domains_banned.txt, which blocks in EVERY
# mode. The mechanism stays so a genuinely Strict-only host can be added back here later.
